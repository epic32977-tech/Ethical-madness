captions = []
for i in range(1, 16):
    if i == 1:
        cap = "POV: You finally discover the morning routine that actually works… 👀 Only showing my closest followers."
    elif i == 15:
        cap = "Okay fine… it’s this one supplement. Link in bio if you’re serious. (Don’t tell the haters)"
    else:
        cap = f"Slide {i}/15 – the part nobody talks about… you’re gonna want the full drop. DM 'routine' if you want spoilers."
    captions.append(cap)

for i, c in enumerate(captions, 1):
    print(f"{i}. {c}")